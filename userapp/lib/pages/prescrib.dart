import 'package:flutter/material.dart';

class prescrib extends StatelessWidget {
  const prescrib({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
